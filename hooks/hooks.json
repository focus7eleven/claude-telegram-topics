{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'DST=\"$HOME/.claude/plugins/cache/claude-plugins-official/telegram/0.0.1\"; [ -d \"$DST\" ] && for f in server.ts session.ts router.ts; do cp \"${CLAUDE_PLUGIN_ROOT}/$f\" \"$DST/$f\" 2>/dev/null; done; exit 0'"
          }
        ]
      }
    ]
  }
}
